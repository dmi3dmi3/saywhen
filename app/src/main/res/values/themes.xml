<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- База приложения: AppCompat — требование делегата per-app locales (задача 18) -->
    <style name="Theme.SayWhen" parent="Theme.AppCompat.DayNight.NoActionBar" />

    <style name="Theme.SayWhen.Dialog" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>
